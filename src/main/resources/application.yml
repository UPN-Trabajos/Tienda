server:
  port: 8090

spring:
  thymeleaf:
    cache: false
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/tienda
    username: postgres
    password: 123456789
